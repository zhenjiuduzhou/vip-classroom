# 本地 V1 验证记录

## 2026-09-18：CF网页部署及首次初始化

- TypeScript检查、前端生产构建通过；86项Vitest通过。
- 初始化覆盖：密钥缺失/错误、来源与CSRF校验、共享限流、有效密码哈希、并发仅创建一个管理员、账号插入失败回滚、拒绝提升已注册普通用户、删除管理员/更换密钥后永久关闭、已有管理员数据库升级关闭。
- 本地D1第0004迁移通过；独立Miniflare/workerd D1执行全部4个迁移，验证真实D1 batch并发、正常管理员登录及删除后永久关闭，通过。没有访问远程D1。
- 1440px和390px初始化浏览器场景通过，检查截图与横向溢出；UI请求模拟，数据库行为另由上项验证。
- CF构建变量生成生产配置后，Wrangler生产部署dry-run通过；固定DB/MEDIA绑定与正式路径路由存在，无运行时Secret写入配置。
- 数据库迁移令牌权限、keep_vars、网页R2 CORS格式及构建/运行变量分工按官方文档核对。未在真实CF网页中操作或发布，不宣称远程部署、免费档CPU、R2线上直传或大陆播放已验收。

以下为历史V1验证，结果数量对应当时版本。

验证日期：2026-09-17。环境：Windows，Node.js 22.14.0，Wrangler 4.133.0。

| 检查 | 结果 |
| --- | --- |
| TypeScript 检查 | 通过 |
| Vitest | 46项通过：密码、联系方式、等级矩阵、到期边界、Range、真实MP4编码校验、清理失败退避及删除并发保护 |
| 本地D1迁移 | 两个迁移通过：核心表及上传、清理任务 |
| 前端生产构建 | 通过，dist输出 |
| Worker部署dry-run | 本地配置及生产模板均通过，未部署远程资源 |
| 本地workerd注册 | 201，强制USER，VIP未设置 |
| 本地会话查询 | 200 |
| 普通用户后台探针 | 403 |
| 错误密码 | 401 |
| 正确密码登录 | 200 |
| 退出及旧会话复用 | 退出200，复用401 |
| 缺少来源/CSRF标记 | 403 |
| Cloudflare账号检查 | Wrangler明确返回未认证 |
| 本地D1/R2集成 | 通过：三片并发32MiB+MP4、完成幂等、未完成拒绝、2GB边界、封面上传、取消、排序、跨课程权限变化、降级、重置密码、重复账号、删除与清理调用 |
| Playwright浏览器验收 | 10个场景全部验证通过；首轮9项通过，修复取消任务显示旧错误的问题后该项复测通过 |
| 视觉检查 | 已查看桌面首页、手机首页、课程后台和用户后台截图，修复装饰图造成的手机横向溢出 |

本地Node.js的三次密码生成耗时408/404/399ms，验证436/391/383ms，进程RSS约99–100MiB。这些是本地墙钟耗时及整个进程内存，**不是Worker CPU时间或隔离实例内存，不能证明免费档通过或失败**。

浏览器场景覆盖桌面首页FAQ、手机首页注册、学员等级锁定、实际视频播放与跳进度/倍速/下一节、退出、到期阻断、普通用户后台阻断、后台创建/封面/多文件队列/排序刷新/移动/删除、用户搜索和有效期修改、手机学员导航、上传失败刷新恢复、取消任务不留小节。

用户后续明确允许先完成本地版本，再自行Cloudflare部署，已完成该阶段源码和页面。未执行远程部署，没有开通付费服务。

仍待线上验证：Cloudflare免费档CPU兼容性、实际R2 S3预签名直传和CORS、正式域名路由及Secure Cookie、中国大陆真实网络下的上传和播放。本地R2采用专用分片适配接口；它的成功不能证明生产S3直传链路通过。2GB边界验证为任务创建校验，不代表实际上传过完整2GB文件。
